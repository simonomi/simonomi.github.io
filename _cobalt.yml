template_extensions:
  - html
  - css
  - json

ignore:
  - LICENSE
  - CNAME
  - giscus.json

site:
  base_url: "https://simonomi.dev"

posts:
  dir: blog
  # i would prefer "drafts", but it renders drafts anyway without the _
  drafts_dir: _drafts

syntax_highlight:
  theme: "css"
  enabled: true
